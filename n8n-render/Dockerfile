# utilise l'image officielle n8n (pinstable :latest ou un tag précis)
FROM n8nio/n8n:latest

# expose le port utilisé par n8n
EXPOSE 5678

# l'image officielle a déjà l'entrypoint, pas besoin de CMD custom.